node_modules/
dist/
*.js.map

# RS256 keys MUST stay out of repo (domain-2 identity signoff §3 + hard rule #2)
keys/
*.pem
*.p8
*.key
*.pub
*.jwk

# Dev secrets
.env
.env.*
!.env.example

# SQLite dev databases
*.db
*.sqlite

# Runtime dev stores (ledger, pseudonym keys, archive, anchor, DSR overlay).
# These hold operational state — including buyer_ids in clear — and must never be committed.
data/

# Pilot launcher state (scripts/pilot.sh): data/, keys/, node.log, minted tokens.
# Holds live keys + buyer_ids in clear — never commit.
.pilot/

# Deploy secrets — the filled-in env for the hosting recipe (deploy/.env.example is tracked).
deploy/.env

# OS
.DS_Store
Thumbs.db
